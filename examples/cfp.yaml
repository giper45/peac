prompt:
  context:
    base:
    - You are a parser
  output:
    web:
      cfp:
        source: http://www.wikicfp.com/cfp/call?conference=computer%20science&page=1
        xpath: //table
  query: Parse and generate markdown
