prompt:
  extends: 
    - docker.yaml
    - networking.yaml
    - use-case-info.yaml
    - sw-iot-device-language-python.yaml
    # - sw-iot-device-language-golang.yaml
    - sw-dashboard-language.yaml
  context:
    base: 
      - Use Docker as Engine for realizing the infrastructure. The CLI agent has a Dockerfile, and the dashboard has separated Dockerfiles
  

  query: Write the source files and the compose