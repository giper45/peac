[tool.poetry]
name = "llm-as-code"
version = "0.1.0"
description = ""
authors = ["gx1 <18548727+giper45@users.noreply.github.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
typer = "^0.12.3"
g4f = {extras = ["all"], version = "^0.3.0.7"}
nodriver = "^0.28rc1"
pyyaml = "^6.0.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"


[tool.poetry.scripts]
llm-as-code = 'llm_as_code.main:app'
